#include <stdio.h>
#include <stdlib.h>

typedef struct
{
    int rows;
    int cols;
    double *data;
} Matrix;

int main(){

    Matrix * m = (Matrix *) malloc(sizeof(Matrix));
    m->rows = 3
    m->cols = 4
    m->data = (double *) malloc(sizeof(double)); 






    return 0;
}